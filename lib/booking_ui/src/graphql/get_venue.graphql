query GetVenue($slug: ID!, $date: Time) {
    getVenue(filter: {slug: $slug}) {
        id
        name
        openingHoursSpecification(date: $date) {
            dayOfWeek
            opens
            closes
        }
    }
}