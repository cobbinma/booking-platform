query GetVenue($venueID: ID!) {
    getVenue(id: $venueID) {
        id
        name
        openingHours {
            dayOfWeek
            validFrom
            validThrough
        }
        specialOpeningHours {
            dayOfWeek
            validFrom
            validThrough
        }
    }
}