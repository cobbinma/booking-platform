mutation CreateBooking($slot: BookingInput!) {
    createBooking(input: $slot) {
        id
        venueId
        email
        people
        startsAt
        endsAt
        duration
        tableId
        givenName
        familyName
    }
}